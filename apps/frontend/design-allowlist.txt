# File FE legacy CHƯA migrate sang design-system token (còn màu raw Tailwind).
# Guard check:design CHỈ cảnh báo cho các file này; file MỚI / file đã migrate phải SẠCH.
# Migrate xong file nào → XOÁ dòng tương ứng. Allowlist rỗng = enforce hoàn toàn.
